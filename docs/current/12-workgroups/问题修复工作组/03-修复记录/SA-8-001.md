# 修复记录: SA-8-001

## 问题信息
- **编号**: SA-8-001
- **来源**: 静态分析工作小组
- **严重程度**: 🔴 P0 - 紧急
- **问题类型**: 稳定性/并发控制
- **发现时间**: 2026-04-10

---

## 问题分析

### 文件位置
`backend/server/src/jobs/environmentSyncJob.js:L254-265`

### 问题代码
```javascript
async function runSync() {
  logger.info('========== 环境数据同步开始 ==========');

  try {
    await generateTasksForAllPlants();
    await fetchWeatherForAllPlants();

    logger.info('========== 环境数据同步完成 ==========');
  } catch (error) {
    logger.error('环境数据同步失败', { error: error.message });
  }
}
```

### 根因分析
- `runSync()` 函数执行时间可能超过2小时（调度周期）
- 无任务重叠保护，上一次未完成时下一次会启动
- 可能导致数据库连接池耗尽、重复生成任务、天气API重复调用

### 影响范围
- 数据库连接池可能被耗尽
- 重复生成相同的 reading_tasks
- 天气API被重复调用，增加成本

---

## 修复方案

### 修复思路
添加并发控制标志 `isSyncRunning` 和 `isCompensationRunning`，在任务执行前检查是否已有任务在运行，防止任务重叠。

### 代码变更

#### 1. 添加并发控制标志
```javascript
let syncIntervalId = null;
let compensateIntervalId = null;

// 并发控制标志
let isSyncRunning = false;
let isCompensationRunning = false;
```

#### 2. 修改 runSync 函数
```javascript
async function runSync() {
  // 并发控制检查
  if (isSyncRunning) {
    logger.warn('上一次同步任务尚未完成，跳过本次执行');
    return;
  }

  isSyncRunning = true;
  logger.info('========== 环境数据同步开始 ==========');

  try {
    await generateTasksForAllPlants();
    await fetchWeatherForAllPlants();

    logger.info('========== 环境数据同步完成 ==========');
  } catch (error) {
    logger.error('环境数据同步失败', { error: error.message });
  } finally {
    isSyncRunning = false;
  }
}
```

#### 3. 修改 runCompensation 函数
```javascript
async function runCompensation() {
  // 并发控制检查
  if (isCompensationRunning) {
    logger.warn('上一次补偿检查尚未完成，跳过本次执行');
    return;
  }

  isCompensationRunning = true;
  logger.info('========== 补偿检查开始 ==========');

  try {
    const count = await compensationService.checkAndCompensateAll();
    logger.info(`========== 补偿检查完成，处理 ${count} 个任务 ==========`);
  } catch (error) {
    logger.error('补偿检查失败', { error: error.message });
  } finally {
    isCompensationRunning = false;
  }
}
```

---

## 修复执行

- **领取人**: AI Assistant
- **领取时间**: 2026-04-11
- **修复人**: AI Assistant
- **修复时间**: 2026-04-11
- **修改文件**: 
  - `backend/server/src/jobs/environmentSyncJob.js`

### 代码 diff
```diff
+ // 并发控制标志
+ let isSyncRunning = false;
+ let isCompensationRunning = false;

  async function runSync() {
+   // 并发控制检查
+   if (isSyncRunning) {
+     logger.warn('上一次同步任务尚未完成，跳过本次执行');
+     return;
+   }
+ 
+   isSyncRunning = true;
    ...
+ } finally {
+   isSyncRunning = false;
  }

  async function runCompensation() {
+   // 并发控制检查
+   if (isCompensationRunning) {
+     logger.warn('上一次补偿检查尚未完成，跳过本次执行');
+     return;
+   }
+ 
+   isCompensationRunning = true;
    ...
+ } finally {
+   isCompensationRunning = false;
+ }
```

---

## 自测验证

### 测试环境
- 本地开发环境
- Node.js 18.x

### 测试项目
- [x] 正常场景：任务正常执行完成
- [x] 并发场景：快速多次调用，验证只有第一次执行
- [x] 异常场景：任务执行出错时标志是否正确重置
- [x] 回归测试：其他定时任务功能正常

### 测试结果
- ✅ 并发控制生效，任务不会重叠执行
- ✅ 任务执行出错时标志正确重置
- ✅ 日志输出正常，可观察到跳过执行的警告

---

## 状态更新

- **当前状态**: ✅ 已修复
- **验证人**: AI Assistant
- **验证时间**: 2026-04-11

---

## 备注

- 使用简单的布尔标志实现并发控制，适用于单实例部署
- 如果未来需要多实例部署，应考虑使用分布式锁（如 Redis）
- 日志中添加了警告信息，便于排查任务执行时间过长的问题
