# 问题核实记录: OM-1-001

## 问题信息
- **编号**: OM-1-001
- **来源**: 运维监控工作小组
- **原严重程度**: 🟠 中危
- **核实后状态**: ⏸️ 已搁置（已修复/误报）
- **问题类型**: 业务逻辑错误

---

## 原问题描述

> `EnvironmentService.getHistoryData` 方法接收到无效的指标代码（weatherCondition, dewPoint, feelsLike），导致环境指标历史数据获取失败

**原影响范围**: 环境指标历史数据获取功能，约20+次错误

**原根因分析**: 前端传递了未定义的指标代码，后端缺少参数校验和容错处理

---

## 实际核实情况

### 核实时间
2026-04-11

### 核实人
AI Assistant

### 代码审查结果

文件: `backend/server/src/services/EnvironmentService.js:L264-294`

```javascript
async getHistoryData(plantId, query = {}) {
  let { metricCode, timeRange = '7d', dataSource } = query;

  // 将 camelCase 转换为 snake_case
  const metricCodeSnake = namingConverter.camelToSnake(metricCode);

  // 验证指标是否存在
  const metric = await EnvironmentMetric.findOne({
    where: { metric_code: metricCodeSnake },
    attributes: ['metric_code', 'name', 'unit', 'icon'],
  });
  
  if (!metric) {
    logger.warn(`[EnvironmentService.getHistoryData] 无效的指标代码: ${metricCode}`);
    // 返回空数据而非抛出错误，避免影响用户体验
    return {
      list: [],
      metricCode: metricCode,
      metricName: metricCode,
      unit: '',
      timeRange,
      message: `不支持的指标类型: ${metricCode}`,
    };
  }
  // ... 正常处理逻辑
}
```

### 结论

代码已经实现了：
1. ✅ **参数校验** - 查询 `EnvironmentMetric` 表验证指标是否存在
2. ✅ **容错处理** - 无效指标时返回空数据和友好消息，不抛出错误
3. ✅ **用户体验保护** - 不会影响功能正常使用

**原错误日志说明**:
- 约20+次错误日志是前端传递未定义指标代码（如 `weatherCondition`, `dewPoint`, `feelsLike`）时的正常警告
- 这些警告日志是预期行为，用于帮助发现前端问题
- 后端已经正确处理，不会导致功能失败

---

## 问题重新定级

| 评估项 | 原评估 | 实际评估 |
|:---|:---|:---|
| **严重程度** | 🟠 中危 | ⏸️ 已修复/误报 |
| **参数校验** | 缺少 | ✅ 已实现 |
| **容错处理** | 缺少 | ✅ 已实现 |
| **功能影响** | 数据获取失败 | ✅ 功能正常 |
| **实际风险** | 中 | 低（仅警告日志） |

---

## 结论

### 原问题状态
**⏸️ 已搁置（已修复/误报）**

### 原因
- 后端代码**已实现参数校验和容错处理**
- 原问题描述"导致环境指标历史数据获取失败"**与事实不符**
- 原错误日志是正常警告，不影响功能

### 后续行动
1. 将此问题标记为已修复/误报
2. 如需减少警告日志，可优化前端不传递未定义指标

---

## 参考文件

- [EnvironmentService.js](../../../../../backend/server/src/services/EnvironmentService.js)

---

**核实时间**: 2026-04-11  
**核实人**: AI Assistant  
**状态**: ⏸️ 已搁置（已修复/误报）
