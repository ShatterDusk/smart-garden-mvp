* [ ] mock- \[x] mock-data.js 中所有字段使用下划线命名

- [x] `cardId` 已改为 `diagnosis_card_id`

- [x] `sessionId` 已改为 `session_id`

- [x] `messageId` 已改为 `message_id`

- [x] `healthScore` 已改为 `health_score`

- [x] `plantName` 已改为 `plant_name`

- [x] `imageUrls` 已改为 `image_urls`

- [x] `createdAt` 已改为 `created_at`

- [x] `sessionList` 已合并到 `sessions`

- [x] `saveSession` 方法使用正确的字段名

- [x] `saveMessages` 方法使用正确的字段名

- [x] `getDiagnosisHistory` 返回正确的字段名

- [x] `quick-analyze.js` 已更新字段引用

- [x] `quick-analyze.wxml` 已更新字段引用

- [ ] `qna.js` 已更新字段引用

- [ ] `sessions.js` 已更新字段引用

- [ ] `plant-detail.js` 已更新字段引用

- [ ] 快速诊断流程可以正常创建会话和消息

