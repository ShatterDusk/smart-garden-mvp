# 会话未读消息红点提示

## 元信息
- **状态**: 进行中
- **优先级**: P2
- **提出时间**: 2026-04-02
- **预计工时**: 4 小时
- **涉及模块**: 前后端

---

## 需求描述

在会话列表页面，为有未读消息的会话显示红点提示。

### 业务场景
1. 用户打开小程序，进入会话列表
2. 有未读消息的会话显示红点
3. 用户点击进入会话后，红点消失
4. 新消息到达时，对应会话显示红点

---

## 实现方案

### 核心思路
采用 **user_config JSON 存储方案**（零数据库变更）：

```javascript
// 在 user_config 表中存储阅读位置
{
  "config_key": "read_positions",
  "config_value": {
    "session_001": "MSG_123456",  // 最后读到的消息ID
    "session_002": "MSG_789012"
  }
}
```

### 判断逻辑

```javascript
// 1. 获取会话的最后一条消息ID
const lastMessage = await Message.findOne({
  where: { session_id: sessionId },
  order: [['created_at', 'DESC']],
  attributes: ['message_id']
});

// 2. 对比用户最后阅读的消息ID
const readPositions = await getUserConfig(userId, 'read_positions') || {};
const hasUnread = lastMessage && lastMessage.message_id !== readPositions[sessionId];
```

---

## 后端实现

### 1. 修改会话列表接口

**文件**: `server/src/controllers/sessionController.js`

在 `getSessionList` 函数中，为每个会话添加 `hasUnread` 字段：

```javascript
// 获取用户阅读位置配置
const readPositions = await getUserConfig(userId, 'read_positions') || {};

// 为每个会话查询最后一条消息并判断是否已读
const sessionsWithUnread = await Promise.all(
  sessions.map(async (session) => {
    const lastMessage = await Message.findOne({
      where: { session_id: session.session_id },
      order: [['created_at', 'DESC']],
      attributes: ['message_id']
    });

    const hasUnread = lastMessage &&
      lastMessage.message_id !== readPositions[session.session_id];

    return {
      ...session.toJSON(),
      hasUnread
    };
  })
);
```

### 2. 添加更新阅读位置接口

**文件**: `server/src/controllers/sessionController.js`

新增 `updateReadPosition` 函数：

```javascript
/**
 * 更新会话阅读位置
 * POST /api/sessions/:id/read
 */
async function updateReadPosition(req, res, next) {
  try {
    const { id: sessionId } = req.params;
    const { userId } = req.user;

    // 获取会话最后一条消息
    const lastMessage = await Message.findOne({
      where: { session_id: sessionId },
      order: [['created_at', 'DESC']],
      attributes: ['message_id']
    });

    if (!lastMessage) {
      return res.success({ message: '无消息可标记' });
    }

    // 更新阅读位置
    const readPositions = await getUserConfig(userId, 'read_positions') || {};
    readPositions[sessionId] = lastMessage.message_id;

    await setUserConfig(userId, 'read_positions', readPositions);

    res.success({ message: '已标记为已读' });
  } catch (error) {
    next(error);
  }
}
```

### 3. 路由配置

**文件**: `server/src/routes/sessions.js`

```javascript
// 标记会话已读
router.post('/:id/read', authMiddleware, asyncHandler(sessionController.updateReadPosition));
```

---

## 前端实现

### 1. 会话列表页面

**文件**: `pages/sessions/sessions.js`

在 `loadSessions` 函数中处理红点状态：

```javascript
async loadSessions() {
  const res = await api.getSessionList();
  const sessions = res.data.map(session => ({
    ...session,
    showUnreadDot: session.hasUnread
  }));
  this.setData({ sessions });
}
```

### 2. 会话列表 UI

**文件**: `pages/sessions/sessions.wxml`

```xml
<view class="session-item" data-id="{{item.session_id}}" bindtap="enterSession">
  <view class="session-info">
    <text class="session-title">{{item.title}}</text>
    <text class="session-preview">{{item.lastMessage}}</text>
  </view>
  <view class="session-meta">
    <text class="session-time">{{item.lastMessageTime}}</text>
    <!-- 未读红点 -->
    <view wx:if="{{item.showUnreadDot}}" class="unread-dot"></view>
  </view>
</view>
```

**文件**: `pages/sessions/sessions.wxss`

```css
.unread-dot {
  width: 8px;
  height: 8px;
  background-color: #ff4d4f;
  border-radius: 50%;
  margin-left: 8px;
}
```

### 3. 进入会话时标记已读

**文件**: `pages/qna/qna.js`

在 `onLoad` 或 `onShow` 中调用：

```javascript
async onLoad(options) {
  const { sessionId } = options;
  this.setData({ sessionId });

  // 加载消息
  await this.loadMessages();

  // 标记会话为已读
  await api.markSessionAsRead(sessionId);
}
```

### 4. API 封装

**文件**: `utils/api.js`

```javascript
// 标记会话已读
markSessionAsRead(sessionId) {
  return this.request({
    url: `/sessions/${sessionId}/read`,
    method: 'POST'
  });
}
```

---

## 注意事项

1. **AI 会话只需要红点提示**，不需要精确未读计数
2. **用户进入会话时更新 `read_positions`**
3. **新消息到达时对比最后阅读位置**判断是否显示红点
4. **性能优化**: 会话列表查询时，可以用子查询优化最后消息获取

---

## 相关文件

| 类型 | 文件路径 |
|:---|:---|
| 后端控制器 | `server/src/controllers/sessionController.js` |
| 后端路由 | `server/src/routes/sessions.js` |
| 前端页面 | `pages/sessions/sessions.js` |
| 前端页面 | `pages/sessions/sessions.wxml` |
| 前端页面 | `pages/sessions/sessions.wxss` |
| 前端页面 | `pages/qna/qna.js` |
| API 封装 | `utils/api.js` |

---

## 更新记录

| 日期 | 版本 | 变更内容 |
|:---|:---:|:---|
| 2026-04-02 | v1.0 | 创建 TODO，确定 user_config JSON 存储方案 |
| 2026-04-05 | v1.1 | 修复 Sequelize JSON 字段更新问题：创建新对象确保变更检测 |

---

## 已知问题修复

### 问题：红点始终显示

**现象**：进入会话后返回列表，红点仍然显示。

**原因**：Sequelize 的 JSON 字段更新时，直接修改对象属性不会触发变更检测。

**错误代码**：
```javascript
const readPositions = readPositionConfig.config_value || {};
readPositions[sessionId] = lastMessage.message_id;
await readPositionConfig.update({ config_value: readPositions });
```

**修复代码**：
```javascript
// 创建新对象以确保 Sequelize 检测到 JSON 字段变化
const readPositions = { ...(readPositionConfig.config_value || {}) };
readPositions[sessionId] = lastMessage.message_id;
await readPositionConfig.update({ config_value: readPositions });
```

